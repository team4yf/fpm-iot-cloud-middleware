/**
 * The dd protocol DD
 */
const _ = require('lodash');
const assert = require('assert');
const debug = require('debug')('fpm-iot-cloud-middleware:protocol-dd');

module.exports = ( deviceId, gatewayId, header, payload ) => {
  
}